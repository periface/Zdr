<div ng-controller="app.views.home as vm">

    <link href="~/App/Main/views/home/googleMapsStyle.css" rel="stylesheet"/>
    <div class="row">
        <div class="col-md-12 col-sm-12">
            <button class="btn btn-primary" ng-click="vm.createZdrModal()">Agregar</button>

            <div class="pull-right">País: <span class="label label-default">{{vm.userLocation.Country.long_name}}</span>  Estado: <span class="label label-default">{{vm.userLocation.State.long_name}}</span>  Ciudad: <span class="label label-default">{{vm.userLocation.City.long_name}}</span></div>
            <div id="mapContainer" class="jumbotron" style="height: 500px">
                <ui-gmap-google-map events="{dragend : vm.setMarker,center_changed:vm.changeCenter,zoom_changed:vm.setMarker}" center='vm.map.center' zoom='vm.map.zoom'>
                    <div ng-repeat="marker in vm.markers">
                        <ui-gmap-marker coords="marker.coords" modelsbyref="false" options="marker.options" events="marker.events" idkey="marker.id">
                        </ui-gmap-marker>
                    </div>
                </ui-gmap-google-map>
            </div>

        </div>
    </div>
</div>